# Frontend (React + TypeScript + MUI)

Run the frontend development server:

```bash
cd frontend
npm install
npm run dev
```

This starts Vite at `http://localhost:5173` by default. The dev server proxies to the backend `/upload` when you call that endpoint directly; run the backend separately as described in the repo README.
