# Copy to .env and fill values to use a real SMTP provider.
# If you leave these empty, the server will use a Nodemailer ethereal test account and print preview URLs.
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
DEFAULT_FROM=beats@yourdomain.com
